{
  "name": "projektarbeit",
  "private": true,
  "license": "BSD-3-Clause",
  "devDependencies": {
    "polymer-cli": "^1.5.2"
  },
  "scripts": {
    "start": "polymer serve --open",
    "build": "polymer build && xcopy /s build\\es5-bundled docs",
    "postinstall": "bower install"
  },
  "dependencies": {
    "redux": "^3.7.2"
  }
}
